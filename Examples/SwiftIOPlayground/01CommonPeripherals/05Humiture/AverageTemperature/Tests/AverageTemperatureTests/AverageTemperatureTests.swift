import XCTest
@testable import AverageTemperature

final class AverageTemperatureTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AverageTemperature().text, "Hello, World!")
    }
}
