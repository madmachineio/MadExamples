import XCTest
@testable import PotentiometerBuzzer

final class PotentiometerBuzzerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PotentiometerBuzzer().text, "Hello, World!")
    }
}
