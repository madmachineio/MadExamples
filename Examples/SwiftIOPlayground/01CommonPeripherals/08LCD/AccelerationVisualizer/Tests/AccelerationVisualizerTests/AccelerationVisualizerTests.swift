import XCTest
@testable import AccelerationVisualizer

final class AccelerationVisualizerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AccelerationVisualizer().text, "Hello, World!")
    }
}
