import XCTest
@testable import C01S04PotentiometerBuzzer

final class C01S04PotentiometerBuzzerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(C01S04PotentiometerBuzzer().text, "Hello, World!")
    }
}
