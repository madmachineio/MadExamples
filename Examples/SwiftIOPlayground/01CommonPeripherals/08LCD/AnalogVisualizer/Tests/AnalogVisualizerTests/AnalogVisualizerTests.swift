import XCTest
@testable import AnalogVisualizer

final class AnalogVisualizerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AnalogVisualizer().text, "Hello, World!")
    }
}
