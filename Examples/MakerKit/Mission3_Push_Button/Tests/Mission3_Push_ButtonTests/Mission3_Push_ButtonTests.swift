import XCTest
@testable import Mission3_Push_Button

final class Mission3_Push_ButtonTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Mission3_Push_Button().text, "Hello, World!")
    }
}
